import Link from "next/link"; import { AppShell } from "@/components/AppShell"; import { modules } from "@/data/curriculum";
export default function Courses(){return <AppShell active="Training"><div style={{padding:"46px 4vw 70px",maxWidth:1250,margin:"auto"}}><div className="eyebrow">Course catalog</div><h1 className="display" style={{fontSize:40,margin:"14px 0 10px"}}>Build the habits behind the result.</h1><p style={{color:"var(--muted)",fontSize:17,maxWidth:680,lineHeight:1.6}}>Work through the core path in order. Every module combines a short lesson, practical application, and a knowledge check.</p><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(300px,1fr))",gap:18,marginTop:36}}>{modules.map(m=><Link href={m.status==="locked"?"#":`/courses/${m.slug}`} className="card" key={m.id} style={{padding:26,minHeight:230,display:"flex",flexDirection:"column",opacity:m.status==="locked"?.72:1}}><div style={{display:"flex",justifyContent:"space-between"}}><span className="eyebrow">Module {String(m.id).padStart(2,"0")}</span><span style={{fontSize:12,fontWeight:800,color:m.status==="active"?"var(--green)":"var(--muted)"}}>{m.status==="active"?"IN PROGRESS":m.status==="available"?"AVAILABLE":"LOCKED"}</span></div><h2 style={{fontFamily:"Georgia,serif",fontSize:25,margin:"24px 0 12px"}}>{m.title}</h2><p style={{color:"var(--muted)",fontSize:14,lineHeight:1.55,margin:0}}>{m.description}</p><span style={{marginTop:"auto",paddingTop:22,fontWeight:900,color:m.status==="locked"?"#9aa7a2":"var(--green)"}}>{m.status==="locked"?"Complete the prior module":"Open module →"}</span></Link>)}</div></div></AppShell>}
