"use client"; import { useState } from "react"; import { AppShell } from "@/components/AppShell";
export default function Practice(){const [step,setStep]=useState(0);const prompts=["Write the thought that usually shows up right before you avoid making a call.","Rewrite that thought using only facts—remove predictions and judgments.","Write one action you can control on the next call."];return <AppShell active="Practice"><div style={{padding:"46px 4vw 70px",maxWidth:950,margin:"auto"}}><div className="eyebrow">Guided practice · 10 minutes</div><h1 className="display" style={{fontSize:55,margin:"14px 0 10px"}}>Reframe the rejection.</h1><p style={{color:"var(--muted)",fontSize:17,lineHeight:1.6,maxWidth:700}}>Turn an automatic fear response into a specific, useful next action. Your response is saved to your private practice journal.</p><div className="card" style={{marginTop:32,overflow:"hidden"}}><div style={{background:"var(--green)",padding:28,color:"white",display:"flex",justifyContent:"space-between"}}><div><small style={{color:"var(--lime)",fontWeight:900}}>SCENARIO</small><h2 style={{fontFamily:"Georgia,serif",fontSize:27,margin:"8px 0 0"}}>The first call block of the day</h2></div><span style={{fontSize:13,color:"#c9dbd5"}}>Step {step+1} of 3</span></div><div style={{padding:36}}><p style={{fontSize:20,fontFamily:"Georgia,serif",lineHeight:1.5,marginTop:0}}>{prompts[step]}</p><textarea placeholder="Write your response here..." style={{width:"100%",minHeight:170,border:"1px solid var(--line)",borderRadius:12,padding:17,resize:"vertical",background:"#fbfcf9",lineHeight:1.6}}/><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginTop:22}}><button className="btn btn-outline" disabled={step===0} onClick={()=>setStep(Math.max(0,step-1))}>← Back</button><div style={{display:"flex",gap:7}}>{prompts.map((_,i)=><span key={i} style={{width:i===step?24:8,height:8,borderRadius:9,background:i===step?"var(--green)":"#dce2db"}}/>)}</div><button className="btn btn-primary" onClick={()=>setStep(Math.min(2,step+1))}>{step===2?"Save reflection":"Continue →"}</button></div></div></div></div></AppShell>}
